import 'package:flutter/material.dart';

void main() => runApp(Matoon());

class Matoon extends StatelessWidget {
  const Matoon({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
