name: register
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  data:
    path: ../../layers/data
  domain:
    path: ../../layers/domain
  common:
    path: ../../libraries/common
